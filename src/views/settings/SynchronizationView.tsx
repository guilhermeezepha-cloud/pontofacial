import React from 'react';

import SynchronizationContainer from '../../features/settings/SynchronizationContainer';

const SynchronizationView = () => {
  return <SynchronizationContainer />;
};

export default SynchronizationView;
