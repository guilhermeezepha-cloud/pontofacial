import React from 'react';

import EmployeeSyncContainer from '../../features/settings/EmployeeSyncContainer';

const EmployeeSyncView = () => {
  return <EmployeeSyncContainer />;
};

export default EmployeeSyncView;
