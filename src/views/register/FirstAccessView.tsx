import React from 'react';

import FirstAccessContainer from '../../features/register/FirstAccessContainer';

const FirstAccessView = () => {
  return <FirstAccessContainer />;
};

export default FirstAccessView;
