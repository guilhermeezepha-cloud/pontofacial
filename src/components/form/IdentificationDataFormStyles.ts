import {StyleSheet} from 'react-native';

import {Colors} from '../../styles/Themes';

const IdentificationDataFormStyles = (colors: Colors) =>
  StyleSheet.create({
    container: {
      gap: 22,
    },
  });

export default IdentificationDataFormStyles;
